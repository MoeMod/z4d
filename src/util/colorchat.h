#pragma once

namespace util{
    inline namespace colorchat{
        inline void PrintColorChat(int id, const char *message)
        {
            // TODO
            sm::PrintToChat(id, message);
        }
    }
}